(ns nquick.core-test
  (:require [clojure.test :refer :all]
            [nquick.core :refer :all]
            [nquick.util :as :util]
            [nquick.commands :as :commands]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 1 1))))